# Backend env (공식 FastAPI와 이름만 맞춤)

이 폴더는 공식 `20_src/backend`를 대체하지 않는다.  
가입 동의는 프론트 + Supabase RLS로 동작한다. FastAPI 라우트를 새로 넣지 않아도 된다.

환경 변수 이름은 `90_config/env/backend.env.example`과 같다.
`.env.example`을 복사해 값을 넣는다. 시크릿은 git에 커밋하지 않는다.
